export function notFound(req,res,next){const err=new Error(`Route not found: ${req.originalUrl}`);err.statusCode=404;next(err)}
export function errorHandler(err,req,res,next){if(err.code==='LIMIT_FILE_SIZE'){err.statusCode=413;err.message='Image must be no larger than 5 MB';}const status=err.statusCode||500;const message=status===500&&process.env.NODE_ENV==='production'?'Something went wrong':err.message||'Something went wrong';if(process.env.NODE_ENV!=='test')console.error(err.message);res.status(status).json({success:false,message, ...(process.env.NODE_ENV==='development'&&status===500?{stack:err.stack}:{})})}
