import jwt from 'jsonwebtoken';import Admin from '../models/Admin.js';import AppError from '../utils/AppError.js';
export async function protect(req,res,next){try{const token=req.cookies?.flaash_token||req.headers.authorization?.replace('Bearer ','');if(!token)throw new AppError('Authentication required',401);const decoded=jwt.verify(token,process.env.JWT_SECRET);const user=await Admin.findById(decoded.id);if(!user||!user.isActive)throw new AppError('Your session is no longer active',401);req.user=user;next()}catch(e){next(e.name==='JsonWebTokenError'||e.name==='TokenExpiredError'?new AppError('Invalid or expired session',401):e)}}
export const allowRoles=(...roles)=>(req,res,next)=>roles.includes(req.user.role)?next():next(new AppError('You do not have permission for this action',403));
